# Weave DeepSeek 配置（精简版）。完整可选项见 docs/config-reference.yaml。

llm:
  provider: deepseek
  model: ${WEAVE_MODEL:-deepseek-chat}
  # api_key 从环境变量 DEEPSEEK_API_KEY 或 OPENAI_API_KEY 读取

loop:
  type: iterative
  max_iterations: 10

prompts:
  system: prompts/system.md
